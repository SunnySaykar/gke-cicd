apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: demo-btxmvxo
  labels:
    app: cicd
description: cicd delivery pipeline
serialPipeline:
  stages:
  - targetId: poctesting-cluster
    # profiles:
    # - dev
  # - targetId: staging
  #   profiles:
  #   - staging
  #   profiles:
  #   - prod
